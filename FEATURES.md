# 고급 기능 설계

## 사진·링크 첨부 (현재 실행하지 않음)

핀에 사진을 붙이면 사진 파일 자체는 Supabase Storage의 비공개 버킷(예: `pin-media`)에 저장한다. DB에는 파일 본문 대신 `pin_attachments` 테이블의 파일 경로, 파일명, 타입, 크기, 업로드한 사용자만 저장한다. 화면에는 짧은 유효기간의 signed URL을 발급해 멤버만 사진을 볼 수 있게 한다.

링크는 `pin_links` 테이블에 URL, 표시 제목, 핀 ID를 저장한다. 열 때는 새 탭을 쓰고 `rel="noopener noreferrer"`를 적용한다. 악성 링크를 막기 위해 `http`와 `https`만 허용한다.

## 핀 태그 (현재 실행하지 않음)

`tags` 테이블에 `맛집`, `숙소`, `관광`, `쇼핑`, `교통`, `사진 명소` 같은 태그를 두고, `pin_tags(pin_id, tag_id)` 연결 테이블로 여러 태그를 한 핀에 붙인다. 목록·지도에서 태그별 필터가 가능해진다. 각 공간의 소유자는 자신만의 태그도 만들 수 있다.

## 실제 자동차·도보 예상 시간 (현재 실행하지 않음)

현재 경로는 핀을 직선으로 연결해 거리만 보여 준다. 실제 도로 경로는 선택된 핀의 위도·경도를 순서대로 `Google Routes API` 또는 Mapbox Directions API에 보낸다. 응답으로 도로를 따르는 선(polyline), 총 거리, 총 시간, 구간별 시간을 받는다.

- 무료 위주: OpenRouteService 등 무료 한도가 있는 라우팅 제공자
- 정확도·안정성 우선: Google Routes API (결제 계정·사용량 과금 필요)
- 저장: 핀 순서만 DB에 저장하고, 예상 시간은 요청 때 계산하거나 짧게 캐시한다. 교통 상황은 계속 바뀌므로 장기 저장값으로 쓰지 않는다.

## 현재 구현되는 고급 기능

텍스트 댓글만 사용할 경우 `comments-migration.sql`만 실행하면 된다. 사진이나 파일은 저장하지 않으므로, 핀 댓글은 무료 플랜에서도 데이터 사용량 부담이 작다.

`features-migration.sql` 실행 후 아래 데이터가 Supabase에 저장되고 같은 여행 공간 멤버에게 실시간 공유된다.

- 공간 삭제 (소유자만, 2회 확인)
- 공간 이름 중복 방지 (같은 소유자 기준)
- 핀 수정·삭제·정렬
- 핀별 댓글
- 새 핀·새 채팅·새 댓글·경로 변경 알림
- 공유 경로와 경로의 핀 순서
