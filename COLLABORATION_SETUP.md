# 협업 기능 적용 기록 (2026-07-23)

이번 작업에서는 외부 초대 방식과 초대 링크 관리는 변경하지 않았습니다.

## 추가된 기능

- 이름 있는 공유 경로: 한 여행 공간 안에 `1일차 이동`, `공항 이동`처럼 여러 경로를 생성하고, 핀을 순서대로 연결합니다.
- 공유 저장: 경로와 순서는 브라우저가 아니라 Supabase DB에 저장되어 같은 공간 멤버에게 실시간으로 보입니다.
- 실제 차량 예상시간: OSRM 경로 API로 실제 도로를 따라 선을 그리고 차량 예상시간·거리를 표시합니다. API 키는 필요 없지만 공개 무료 서버이므로 대량 사용 서비스에서는 별도 경로 API로 교체해야 합니다.
- 핀 태그: 핀 추가/수정 시 쉼표로 최대 5개 태그를 입력하고, 태그 필터 및 통합 검색으로 찾습니다.
- 채팅 읽음: 채팅 탭을 열면 읽음 처리되며, 본인 메시지에는 읽은 인원 수가 표시됩니다. 읽지 않은 메시지 수는 채팅 탭에 배지로 표시됩니다.
- 권한 UI: 핀 수정/삭제 버튼은 작성자와 공간 소유자에게만 표시됩니다.
- 저장 직후 새로고침: 핀·채팅을 보낸 뒤 실시간 연결 상태와 관계없이 본인 화면을 즉시 갱신합니다.

## 반드시 한 번 할 일

Supabase Dashboard → **SQL Editor**에서 아래 파일 전체를 붙여 넣고 **Run** 합니다.

`supabase/collaboration-migration.sql`

이 파일은 댓글, 공간 삭제 권한, 공간 이름 중복 방지, 태그, 공유 경로, 채팅 읽음, 알림 테이블과 실시간 설정을 함께 만듭니다.

주의: 예전에 만들어 둔 `features-migration.sql`은 실행하지 않습니다. 새 파일이 여러 이름의 경로를 지원하는 구조입니다.

## 확인 순서

1. 한 계정으로 여행 공간과 `1일차 이동` 경로를 만듭니다.
2. 핀 3개를 경로 지정 모드에서 차례대로 누르고 다른 계정에서 같은 공간을 엽니다.
3. 두 번째 계정에서 새 채팅을 확인하고 읽음 배지와 읽음 수를 확인합니다.
4. 태그를 넣어 만든 핀을 태그 필터와 검색으로 찾아봅니다.
