# 작업 로그 — 실시간 지도 공유

최종 갱신: 2026-07-23

## 이 프로젝트의 목표

초대받은 사람만 입장하는 지도 공유 웹서비스를 만든다. 참여자는 여행·모임처럼 분리된 지도 공간에서 핀과 메모를 남기고, 채팅하며, 거리를 확인한다. `전체 지도`에서는 본인이 권한을 가진 여러 공간의 핀을 모아 본다.

## 현재 구현 방식

현재 단계는 서버를 붙이기 전의 브라우저 프로토타입이다. 별도 설치·API 키·유료 서비스 없이 `index.html`을 브라우저에서 열어 바로 확인할 수 있다.

> 2026-07-23 업데이트: 실제 운영용 앱을 `webapp/`에 별도로 작성했다. 이 앱은 Supabase URL과 publishable key를 연결하면 회원가입, 공간, 핀, 채팅, 초대, 실시간 동기화를 사용한다.

| 파일 | 역할 |
| --- | --- |
| `index.html` | 화면 구조와 대화상자 |
| `styles.css` | PC·모바일 반응형 디자인 |
| `app.js` | 지도, 핀, 거리, 채팅 UI, 닉네임, 공간 전환 동작 |
| `README.md` | 요구사항과 기술 방향 |

## 선택한 무료 기술

- 지도: Leaflet + OpenStreetMap 타일
- 현재 위치: 브라우저 Geolocation API (사용자가 허용할 때만)
- 닉네임 저장: 브라우저 `localStorage`

이 구성은 지도 표시를 위한 Google Maps API 키나 결제 계정이 필요 없다. 단, 지도 타일을 가져오기 위해 인터넷 연결은 필요하다.

## 구현 완료 항목

- PC와 모바일에 대응하는 지도 화면
- 지도 클릭으로 핀 추가, 핀 이름·메모·색상 입력
- 핀 두 개 선택 후 직선거리 표시
- 채팅 화면 UI와 메시지 작성
- 현재 위치 버튼: 사용자가 브라우저 위치 권한을 허용할 때만 위치 표시
- 접속 시 닉네임 입력; 같은 브라우저에서 다시 열면 저장된 닉네임 사용
- 지도 공간 전환
  - `한국 여행`: 한국 여행 핀만 표시
  - `중국 여행`: 중국 여행 핀만 표시
  - `전체 지도`: 두 공간의 핀을 합쳐 표시

## 지금은 아직 임시인 부분

- 핀·채팅·공간 데이터는 `app.js` 메모리에 있다. 새로고침하면 새로 추가한 데이터가 사라진다.
- 닉네임은 현재 기기의 브라우저에만 저장된다.
- 초대, 로그인, 다른 사용자와 실시간 동기화는 아직 연결하지 않았다.
- 공간별 채팅 분리와 장소 검색·도로 경로는 다음 구현 항목이다.

## 다음 권장 작업 순서

1. Supabase 프로젝트 생성 및 무료 플랜 연결
2. 이메일 또는 Google 로그인 추가
3. 데이터베이스 테이블 생성: 공간, 멤버, 핀, 메모, 채팅
4. Row Level Security로 초대된 멤버만 해당 공간의 데이터를 읽도록 설정
5. 지금의 브라우저 임시 데이터를 Supabase 실시간 데이터로 교체
6. 초대 링크, 공간 생성, 공간별 채팅 분리
7. 장소 검색과 경로 지정 추가

## 다음 세션 시작 문장

`바탕화면 실시간지도공유 폴더의 WORKLOG.md와 README.md를 읽고, Supabase를 연결하는 다음 단계를 진행해줘.`

## 운영용 앱 파일

| 경로 | 역할 |
| --- | --- |
| `webapp/index.html` | 실제 서비스 화면 |
| `webapp/app.js` | Supabase 인증·공간·핀·채팅·초대·실시간 처리 |
| `webapp/config.js` | 사용자가 입력할 Supabase 공개 연결 정보 |
| `webapp/server.mjs` | 별도 설치 없이 로컬에서 실행하는 개발 서버 |
| `supabase/schema.sql` | 데이터베이스·권한(RLS)·초대·실시간 설정 |
| `SETUP.md` | Supabase 연결·실행·배포 방법 |

## 2026-07-23 완성본 점검

- 정적 서버로 `webapp/index.html`을 제공해 HTTP 200 응답을 확인했다.
- `webapp/app.js`와 `webapp/server.mjs`의 JavaScript 문법 검사를 통과했다.
- 아직 Supabase 프로젝트 정보는 의도적으로 입력하지 않았다. 계정의 URL과 publishable key는 사용자 계정에 속하므로, 이 값이 연결되기 전에는 설정 화면이 표시되는 것이 정상이다.

## 사용자에게 필요한 다음 한 단계

`SETUP.md`의 1단계에 따라 무료 Supabase 프로젝트를 생성하고 `schema.sql`을 실행한 뒤, `webapp/config.js`에 URL과 publishable key를 넣는다. 그 다음에는 로컬 실행 또는 배포를 실제로 함께 진행한다.

### 추가 기능 SQL

- 핀 댓글만 쓸 때: `supabase/comments-migration.sql`
- 여행 공간 삭제·같은 이름 중복 방지: `supabase/space-delete-migration.sql`

## 2026-07-23 협업·모바일 개선 저장 지점

- 모바일 핀 목록 레이어와 하단 지도 조작 UI를 수정했다.
- 핀 태그 검색·필터, 긴 메모 축약 표시·수정, 댓글/핀 생성 시각 표시를 추가했다.
- 두 핀 경로는 도로 경로 API로 표시하며, 현재 경로는 같은 공간 멤버에게 공유하도록 변경했다.
- 채팅 읽음 수, 읽지 않은 채팅 배지, 종 아이콘의 앱 내 알림 수를 추가했다.
- 위치 공유는 연결 상태 기반 Presence 방식으로 변경 중이며, 탭 종료 시 공유 상태가 사라지도록 구성했다.
- Supabase에서 `collaboration-migration.sql`을 실행했다. 읽음 수 즉시 반영을 위해 `message-reads-realtime-migration.sql`을 추가 실행해야 한다.
