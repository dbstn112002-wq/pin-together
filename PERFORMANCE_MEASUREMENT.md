# Pin Together 1차 프론트엔드 성능 개선 기록

측정일: 2026-07-27

## 범위

이번 단계는 기능이나 Supabase RLS 정책을 바꾸지 않고, 현재의 목록 조회 흐름과 사용자가 자주 누르는 반응·즐겨찾기 동작을 가볍게 만드는 데 집중했다.

- 핀 목록의 보조 데이터 조회를 병렬 처리
- 반응과 공통 즐겨찾기에서 전체 목록 재조회 제거
- 핀 검색 입력에 180ms 디바운스 적용
- 브라우저에서 실제 실행 시간을 확인할 수 있는 측정값 추가

마커 재사용, 사진 썸네일 캐시, RPC 통합과 DB 인덱스 조정은 이 단계에 포함하지 않았다.

## 전후 비교

| 동작 | 변경 전 | 변경 후 | 효과 |
| --- | --- | --- | --- |
| `loadPins()` (핀 존재 공간) | 핀 1회 + 태그/반응/댓글/읽음/즐겨찾기/경로 6회를 순서대로 요청. 총 7회, 네트워크 대기 단계 최대 7회 | 동일한 데이터 요청 7회이지만, 핀 1회 후 보조 요청 6회를 동시에 수행. 네트워크 대기 단계 2회 | 느린 모바일 회선에서 보조 요청의 대기 시간이 합산되지 않음 |
| 반응 1회 | 변경 요청 1회 후 `loadPins()` 전체 실행. 총 최대 8회 요청 및 목록·마커 전체 재생성 | 변경 요청 1회 후 해당 핀의 반응 UI만 갱신 | 불필요한 지도/목록 재렌더링 제거 |
| 공통 즐겨찾기 1회 | 변경 요청 1회 후 `loadPins()` 전체 실행. 총 최대 8회 요청 | 변경 요청 1회 후 별표와 개수만 갱신 | 체감 응답성 개선 |
| 검색어 입력 | 입력 글자마다 즉시 전체 목록 렌더링 | 마지막 입력 후 180ms 동안 추가 입력이 없을 때 한 번 렌더링 | 빠르게 입력할 때 렌더링 횟수 감소 |

요청 수는 브라우저 코드의 호출 구조를 기준으로 확인했다. 실제 벽시계 시간(ms)은 로그인한 실제 공간의 데이터량·네트워크 환경에 따라 달라지므로, 배포 후 같은 계정과 공간에서 아래 절차로 전후 값을 비교해야 한다.

## 실제 시간 측정 방법

1. 서비스에 로그인한 뒤 개발자 도구 콘솔에서 다음을 실행한다.

   ```js
   sessionStorage.setItem('pin-together-performance-debug', '1')
   ```

2. 공간을 다시 열거나 새로고침해 핀 목록을 불러온다.
3. 콘솔의 `[performance]` 로그 또는 다음 객체에서 `durationMs`를 확인한다.

   ```js
   window.__pinTogetherPerformance
   ```

4. 같은 공간에서 `loadPins`, `toggleReaction`, `toggleFavorite`을 각각 3회 이상 기록해 중앙값을 비교한다.

예: `window.__pinTogetherPerformance.loadPins.durationMs`

이 측정은 앱이 실제로 기다린 시간을 기록하며, 디버그 키를 설정하지 않으면 콘솔 로그를 남기지 않는다. Safari의 제한된 저장소 환경에서도 서비스 동작에는 영향을 주지 않도록 보호 처리했다.
